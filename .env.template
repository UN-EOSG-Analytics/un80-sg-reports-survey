# Database
DATABASE_URL=postgresql://user:pass@localhost:5432/postgres?sslmode=verify-full
DB_SCHEMA=myapp

# Auth (generate with: openssl rand -hex 32)
AUTH_SECRET=change-me-in-production

# Email (magic link delivery)
SMTP_HOST=smtp.mailbox.org
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@example.com

# Base URL for magic links
BASE_URL=http://localhost:3000

# URL for Digital Library internal endpoint
AWS_API_URL=


AZURE_POSTGRES_HOST=
AZURE_POSTGRES_PORT=
AZURE_POSTGRES_DB=
AZURE_POSTGRES_USER=
AZURE_POSTGRES_PASSWORD=